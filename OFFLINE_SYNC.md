# ValuePlus Offline Sync

ระบบนี้ออกแบบให้ผู้ใช้ทำงานต่อได้เมื่อ Apps Script, Google Drive หรือเครือข่ายสะดุดชั่วคราว โดยแยกการทำงานเป็น 3 ชั้น:

1. **Retry:** คำขอชั่วคราว (`404`, `408`, `425`, `429`, `5xx`, timeout และ response JSON ที่เสีย) จะลองใหม่สูงสุด 5 ครั้งด้วย backoff
2. **Durable Outbox:** ถ้าคำสั่งบันทึกยังไม่สำเร็จ แอปจะเก็บ request และ PDF ลง App Data ก่อน แล้วซิงก์ใหม่ทุก 20 วินาที เมื่อกลับมาออนไลน์ เมื่อหน้าต่างได้ focus หรือเมื่อล็อกอิน
3. **Response Cache:** รายการและ PDF ที่เคยโหลดจะเปิดจากแคชในเครื่องได้เมื่อบริการภายนอกไม่ตอบ โดยจำกัดพื้นที่รวมไว้ที่ 512 MB และลบไฟล์เก่าก่อน

ทุก mutation ใช้ `requestId` เดิมตลอดอายุงาน ฝั่ง Apps Script จำผลคำขอไว้ 6 ชั่วโมงเพื่อป้องกันการบันทึกซ้ำเมื่อคำสั่งสำเร็จแล้วแต่ response สูญหาย

## ตำแหน่งข้อมูล

Tauri จัดเก็บข้อมูลภายใต้ App Data ของ `com.valueplus.system`:

- `apps-script-outbox`: งานที่ยังรอซิงก์ ห้ามลบระหว่างทดสอบ recovery
- `apps-script-cache`: แคชรายการและ Preview ลบได้โดยไม่ทำให้ข้อมูลต้นฉบับหาย

ไฟล์ Outbox จะถูกลบเฉพาะหลัง Apps Script ตอบกลับว่าสำเร็จเท่านั้น

## ลำดับการ Deploy

ต้องทำตามลำดับนี้เสมอ:

1. นำ `google-apps-script/Router.gs` และไฟล์ Apps Script ที่เปลี่ยนขึ้นโปรเจกต์
2. Deploy > Manage deployments > Edit > New version > Deploy
3. ตรวจ `doGet` ของ URL `/exec` และทดสอบล็อกอิน/บันทึก 1 รายการ
4. Build และทดสอบตัวแอปใหม่
5. ปล่อยตัวติดตั้ง/อัปเดตแอป

Router ใหม่ยังรองรับแอปรุ่นเดิมที่ไม่มี `requestId` จึงต้อง Deploy Apps Script ก่อนโดยไม่กระทบผู้ใช้เดิม ส่วนแอปรุ่นใหม่ต้องใช้ Router ใหม่เพื่อให้ replay protection ทำงานครบ

## การทดสอบก่อนปล่อย

1. ออนไลน์: บันทึกและเปิด Preview ตามปกติ
2. ออฟไลน์: ตัดเครือข่ายแล้วบันทึก ต้องเห็น `รอซิงก์ N งาน` และไม่แสดงเป็นงานล้มเหลว
3. Restart recovery: ปิดแอปทั้งที่มีงานรอ เปิดใหม่ ล็อกอินด้วยผู้ใช้เดิม แล้วต่อเครือข่าย งานต้องซิงก์และหายออกจากคิว
4. Duplicate protection: ตัดเครือข่ายระหว่างรับ response แล้วต่อใหม่ ต้องมีข้อมูล/ไฟล์เพียงชุดเดียว
5. Cached read: เปิดแฟ้มและ Preview อย่างน้อยหนึ่งครั้ง ตัดเครือข่าย แล้วเปิดข้อมูลเดิมอีกครั้ง
6. Role isolation: งานของ `OFFICE` ต้องไม่ถูกส่งด้วย session ของ `HEADOFFICE` และกลับกัน

## ข้อควรระวัง

- อย่าล้าง App Data หรือถอนโปรแกรมแบบลบข้อมูลผู้ใช้ขณะที่ยังมีงานรอซิงก์
- ถ้าสถานะคิวไม่ลด ให้ล็อกอินด้วยผู้ใช้เดิมที่สร้างงานแล้วกดปุ่มสถานะซิงก์ด้านบน
- การย้าย URL Apps Script ต้องแก้ค่า endpoint และ Deploy Router ให้เรียบร้อยก่อน Build แอป
