# การควบคุมอัปเดต ValuePlus System

เมื่อเปิดโปรแกรม ระบบจะตรวจสอบอัปเดตก่อนแสดงหน้า Login ทุกครั้ง

## อัปเดตทั่วไป

เลือก `mandatory=false` ตอนสั่ง Publish Update ผู้ใช้จะเห็นปุ่ม
`อัปเดตตอนนี้` และ `ภายหลัง`

## อัปเดตบังคับ

เลือก `mandatory=true` ตอนสั่ง Publish Update ผู้ใช้จะต้องติดตั้งอัปเดตก่อน
จึงจะเข้าใช้งานได้ และระบบจะไม่แสดงปุ่ม `ภายหลัง`

สามารถบังคับเฉพาะเวอร์ชันเก่ามากด้วย `minimum_supported_version` เช่น
กำหนดเป็น `1.0.16` จะบังคับผู้ใช้ที่ต่ำกว่า 1.0.16 แม้ตั้ง `mandatory=false`

## วิธี Publish ผ่าน GitHub

1. เปิดแท็บ **Actions** ของ repository
2. เลือก workflow **Publish ValuePlus Update**
3. กด **Run workflow**
4. เลือกว่าจะบังคับอัปเดตหรือไม่ กรอกเวอร์ชันต่ำสุดและข้อความ
5. กด **Run workflow** และรอ build Windows สำเร็จ

> รุ่นที่ติดตั้งก่อนมี Startup Update Gate จะยังบังคับจากระยะไกลไม่ได้
> ผู้ใช้ต้องติดตั้งรุ่นแรกที่มีฟีเจอร์นี้อย่างน้อยหนึ่งครั้งก่อน
